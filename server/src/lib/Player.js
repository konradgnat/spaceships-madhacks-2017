export class Player {
  constructor(id, color, orientation, posX, posY, velX, velY) {
    this.id = id
    this.color = color
    this.orientation = orientation
    this.posX = posX
    this.posY = posY
    this.velX = velX
    this.velY = velY
  }
}
